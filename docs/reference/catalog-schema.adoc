= Reference: Catalog schema

Validate with `pnpm validate:catalog`.

Source files:

* `schemas/lexicon.schema.json`
* `catalog/lexicon.json` (generated by `node scripts/build-catalog.mjs`)

Each term includes meaning, history, parameters, viz kind, audition recipe id, and optional `exports.equalizerApo` / `exports.obs`.
