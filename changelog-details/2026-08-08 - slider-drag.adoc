= Slider drag without re-render

Parameter `input` events no longer call a full DOM `paint()`. Live updates patch value labels, visualization, and export in place so range thumbs keep click-and-drag tracking. Search only rebuilds the tree node.
