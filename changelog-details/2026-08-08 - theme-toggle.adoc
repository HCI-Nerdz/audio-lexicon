= Light/dark theme toggle

Web UI follows `prefers-color-scheme` by default, with a sidebar toggle to lock light or dark. The next OS theme-change event clears the manual lock and returns to system. Includes dark CSS tokens and FOUC-safe early theme script.
